# 成绩核算系统 — 激活码使用说明

## 一、概述

成绩核算系统采用「机器绑定 + 激活码」保护机制：
- 每台电脑有唯一的**机器码**（基于硬件信息生成）
- 管理员根据机器码生成对应的**激活码**
- 支持两种激活码：**永久激活码**（V1 开头）和**试用激活码**（T1 开头，3天有效期）
- 首次运行自动进入 3 天试用期，试用结束后需输入激活码

---

## 二、管理员操作（生成激活码）

### 2.1 获取机器码

让使用单位（学校）打开成绩核算系统，激活页面上会显示本机机器码，例如：

```
30546828a7845a088541be0820eca763
```

让对方把这串机器码发给你（复制粘贴、微信、QQ 均可）。

### 2.2 生成激活码

在你自己的电脑上，使用打包好的管理员工具：

```bash
# 使用 exe 版本（推荐）
激活码生成工具.exe SCHOOL 30546828a7845a088541be0820eca763

# 或使用 Python 版本
python admin_genkey.py SCHOOL 30546828a7845a088541be0820eca763
```

参数说明：
- 第一个参数 `SCHOOL` 是**学校代码**（2-6 位大写字母，自定义，如 SCHOOL=示例学校、ABC=另一学校）
- 第二个参数是对方发来的**机器码**

输出示例：
```
学校代码: SCHOOL
机器码:   30546828a7845a088541be0820eca763
激活码:   V1-SCHOOL-30546828-FBC6A9DF
```

把生成的 `激活码` 发给对方即可。

### 2.3 生成试用激活码（可选）

如果需要给客户生成试用激活码（3天有效期）：

```bash
# 使用 exe 版本（推荐）
激活码生成工具.exe --trial SCHOOL 30546828a7845a088541be0820eca763

# 或使用 Python 版本
python admin_genkey.py --trial SCHOOL 30546828a7845a088541be0820eca763
```

输出示例：
```
学校代码: SCHOOL
机器码:   30546828a7845a088541be0820eca763
试用激活码: T1-SCHOOL-30546828-FBC6A9DF
（试用期: 3天）
```

**注意：** 试用激活码以 `T1-` 开头，永久激活码以 `V1-` 开头。

### 2.4 批量生成（可选）

如果需要一次性给多台电脑生成激活码，可以创建一个文本文件 `machines.txt`，每行一个机器码：

```
30546828a7845a088541be0820eca763
a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6
...
```

然后运行：
```bash
# 批量生成永久激活码
激活码生成工具.exe --batch SCHOOL machines.txt

# 批量生成试用激活码
激活码生成工具.exe --batch-trial SCHOOL machines.txt
```

### 2.5 查看本机机器码（测试用）

```bash
# 使用 exe 版本（推荐）
激活码生成工具.exe --show-machine

# 或使用 Python 版本
python admin_genkey.py --show-machine
```

---

## 三、用户操作（激活系统）

### 3.1 首次使用（试用期）

1. 双击 `成绩核算系统.exe`
2. 浏览器自动打开，显示**激活页面**
3. 页面显示「试用期剩余 3 天」，可直接关闭激活页面开始试用
4. 试用期内可正常使用所有功能

### 3.2 正式激活

试用期结束后，需要输入激活码才能继续使用：

1. 页面上显示本机机器码，点击「复制」按钮
2. 把机器码发给管理员，等待管理员返回激活码
3. 在「激活码」输入框中粘贴激活码，点击「激活」
4. 提示「激活成功！请重新启动程序。」
5. 关闭程序，重新双击 `成绩核算系统.exe`，即可正常使用

### 3.3 激活后

- 永久激活码激活一次永久有效，无需重复操作
- 试用激活码可多次使用，每次重置 3 天试用期
- 激活信息保存在 `config/activation.json`（隐藏文件）
- 不要手动删除该文件，否则需要重新激活

### 3.3 更换电脑

- 每台电脑的机器码不同，换电脑需要重新申请激活码
- 将 `成绩核算系统` 文件夹复制到新电脑后，按上述步骤重新激活

---

## 四、常见问题

### Q: 激活码输入后提示「激活码无效或与本机不匹配」？

**A:** 激活码与机器码是一一对应的。请确认：
- 激活码没有多复制空格或少复制字符
- 激活码是针对这台电脑生成的（不是其他电脑的）
- 学校代码部分正确（如 V1-**SCHOOL**-... 或 T1-**SCHOOL**-...）

### Q: 激活后重装系统/更换硬件，激活会失效吗？

**A:** 会。机器码基于 CPU、主板、硬盘信息生成。更换主要硬件或重装系统后机器码可能变化，需要联系管理员重新生成激活码。

### Q: 管理员工具会被打包进主程序 exe 吗？

**A:** 不会。`chengji.spec` 中没有包含管理员工具，打包后用户拿不到它。管理员需要单独保管 `激活码生成工具.exe` 或 `admin_genkey.py`。

### Q: 能不能一台电脑激活后，把整个文件夹复制到其他电脑直接用？

**A:** 不能。复制到其他电脑后，机器码不同，激活校验会失败，需要重新激活。

### Q: 试用期结束后，删除文件能重置试用期吗？

**A:** 不能。试用时间同时存储在注册表和隐藏文件中，删除文件无法重置试用期。需要使用试用激活码（T1 开头）才能重置。

### Q: 永久激活码和试用激活码有什么区别？

**A:**
- **永久激活码**（V1 开头）：激活一次永久有效，适合正式用户
- **试用激活码**（T1 开头）：每次使用重置 3 天试用期，适合体验用户

---

## 五、管理员注意事项

1. **妥善保管 `激活码生成工具.exe`**，不要随软件一起分发给用户
2. 记录每台电脑的机器码和对应激活码，便于后续维护
3. 学校代码建议统一命名规则（如拼音首字母），方便管理
4. 如需撤销某台电脑的永久授权，让用户删除 `config/activation.json` 即可
5. **试用管理：** 可以给潜在客户发送试用激活码（T1 开头），让他们体验 3 天
6. **批量生成：** 使用 `--batch` 或 `--batch-trial` 参数批量生成激活码
